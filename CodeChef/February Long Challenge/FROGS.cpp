#include<bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);

    int T;
    cin >> T;
    while (T--) {
        int N;
        int W[6], L[6], arr[100]{};
        cin >> N;
        for (int i = 0; i < N; i++) {
            cin >> W[i];
        }
        for (int i = 0; i < N; i++) {
            cin >> L[i];
        }
        
    }

    return 0;
}