#include<bits/stdc++.h>

using namespace std;

string solve() {
    
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    int T;
    cin >> T;

    while(T--) {
        int X, Y;
        cin >> X >> Y;

        cout << solve() << "\n";
    }

    return 0;
}